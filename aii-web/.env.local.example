# AII 后端 base URL(不带尾斜杠)
# Backend base URL (no trailing slash)
NEXT_PUBLIC_AII_API_BASE=http://localhost:8000

# Mock 模式开关 — 后端没起时用 mock 数据
# 设 "true" 启用,设 "false" / 不设走真 API
# Mock toggle — set "true" to use mock data, anything else uses real backend
NEXT_PUBLIC_USE_MOCK=true
