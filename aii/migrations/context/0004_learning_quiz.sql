-- ============================================================================
-- cx.learning_progress.pending_quiz — 服务端答案托管速测 (吸收 DeepTutor, P1.3)
-- LLM 出题时把"标准答案"存服务端(不返回给前端), 学习者作答后由裁判对照标准答案判定——
-- 出题与判分解耦, 出题模型没法在判分时放水/幻觉正确(原则二在事实/记忆类知识上的落地)。
-- 结构: {"question":"...","expected":"...","posed_at":"iso"}; 判完清空。
-- ============================================================================

ALTER TABLE cx.learning_progress
  ADD COLUMN IF NOT EXISTS pending_quiz jsonb;
