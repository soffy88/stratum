/**
 * ConfidenceMeter — aii-web 薄包装。
 * 实现已提升到 @helios/blocks OConfidenceMeter(v1.10.0+)。
 * 保留本文件供 AIIResponse 等组件 import,不改 import 路径。
 */
export { OConfidenceMeter as ConfidenceMeter } from '@helios/blocks';
export type { OConfidenceMeterProps as ConfidenceMeterProps } from '@helios/blocks';
