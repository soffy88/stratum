"""Audio generator agent — disabled v1.0."""


def run(**kwargs):
    raise NotImplementedError(
        "TTS unavailable v1.0: F5-TTS upstream image broken, status=failed, "
        "error='TTS unavailable v1.0'"
    )
