# 抽取候选抽检包（ARC-SPEC §4.5）· 送顾问/Wiki 审重

> **红线**：本目录全为**抽取候选**（`is_gold=false`），**永不直接成 gold**（§4.4）。gold 只出自判定流程（顾问裁）。半自动定档 = qwen2.5vl:7b 产候选、date 留 chronology 注册表、顾问审重（EXTRACTOR-SELECTION-W-H1a.md）。

## W-H1a-1-candidates.json

- **来源**：语料库 16 段白文（已入库子集）经 qwen2.5vl:7b 抽取。
- **结果**：16 候选，person 注册表解析命中 16/24（67%），date 不产出（§4.4 发现：抽取器 date 全废）。
- **管线**：抽取 → 注册表解析核（已跑）→ 同一性判定（P2 接，本波未跑：候选多为已 gold 覆盖事件的再抽取）→ 顾问审重（待）。

## ★诚实标注（§4.5 出口判据未达）

- §4.5 要求『首批自动抽取 **100 事件**（左传·晋新事件优先）』——**未达**。原因：① 全书左传未入库（本波只抓被引段+arc核心，见 corpus/REGISTRY.md），无从抽 100 个**新**事件；② 抽取器未定型全自动（半自动定档，Qwen3 候选 pull 被代理拦）。
- 本包 16 候选系**管线演示 + 半自动样本**（多为已 gold 覆盖事件的再抽取，用以验证 抽取→注册表解析 链路），**非 100 新事件**。full-100 BLOCKED 于：全书左传入库 + 抽取器全自动定型（均 PENDING）。

---

## W-H1a-2-candidates.json（qwen3-8b 定型后重跑）

- 抽取器升级 qwen2.5vl:7b → **qwen3-8b**（定型，字段均 0.94 vs 0.71）；title 现有意义（智果别族/阳平之战/田氏杀简公/鸿门宴）。
- 19 候选，person 注册表解析 23/40，全 is_gold=false，date 不产出（D-028）。
- **100 新事件仍未达**：语料 19 段（多为已 gold 覆盖事件的再抽取），非 100 个左传晋系**新**事件——full-100 BLOCKED 于全书左传入库（PENDING）。本包为定型抽取器的管线复跑样本。
