# W-H1a-4 左传晋系事件批（P1 百事件推进）· 送裁

> `W-H1a-4-events.json`（55）+ `W-H1a-3-arc-candidates.json`（34）= **累计 86 唯一 para 事件候选**（近 100 目标）。全 is_gold=false，date 不产出（D-028），person 走 PERSON-INTAKE 协议。
> **诚实**：86/100——采样重叠致净增有限；全文左传在库、`extract_arc_batch.py` 在，**续跑至 100+ 无阻**（调 N/offset）。本波未凑满 100（预算），标 PENDING-补齐。
> 结构质量：两批 valid-JSON + type-enum 均 **100%**。person 解析率：34 晋系段扩容后 76% / 广采样 55 段 22%（解析率随注册表覆盖广度浮动，趋势见 PERSON-INTAKE 报告）。
