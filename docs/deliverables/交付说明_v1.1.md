# Stratum 前端交付物 v1.1 — 文档列表 + AI 处理选项

P0:文档列表空白(ODocumentTree stub)修复 + 入库 AI 处理选项。6 个文件,放进 stratum-web 对应路径。

## 文件清单
```
src/lib/documents.ts                    listDocuments + getDerivatives + Substrate/Derivative 类型
src/components/AIProcessingOptions.tsx  AI 处理选项共用组件(markdown 必选,concepts disabled)
src/app/(app)/documents/page.tsx        替换 ODocumentTree → 真实列表(图标+标题+类型/来源+4衍生品标签+分页+View Banner)
src/components/UploadDialog.tsx         上传文件 + AI 选项(FormData: file + generate_derivatives JSON string)
src/components/UrlIngestDialog.tsx      URL 抓取 + AI 选项(POST /inbox/submit {url, generate_derivatives})
src/components/FolderIngestDialog.tsx   文件夹监听 + AI 选项(POST /folder-watch {path, description, generate_derivatives})
```

## 要点
- ODocumentTree 已彻底删除,documents 页用真实列表(160+ 本书可见)
- 衍生品标签 [MD/译/音频/插图],已生成绿色 ✓,未生成灰色 –(调 GET /documents/{id}/derivatives)
- 分页 50 条/页,URL 带 ?page=N
- AI 处理选项:Markdown 不可取消(基础),concepts disabled 标"即将推出"
- 三个入库 Dialog 共用 AIProcessingOptions 组件
- 移动端响应式,按钮 min-h-44px,深浅主题
- documents 页的「上传/URL/RSS/文件夹」按钮已就位,接 Dialog 时把对应 Dialog 的 open 状态接上即可(Dialog 组件已独立可用)

## 后端需补(§3)
- GET /api/v1/documents/{id}/derivatives → [{kind}]
- POST /api/v1/inbox/submit 加 generate_derivatives 字段
- POST /api/v1/folder-watch 加 generate_derivatives 字段

已通过 TypeScript 类型检查(tsc 零错误)。
